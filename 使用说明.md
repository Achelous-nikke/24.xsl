# 24点 · 田园手作版

## 直接运行

解压压缩包后，双击 `index.html` 即可在浏览器中打开。整个游戏不依赖网络。

## 操作方法

1. 依次点击第一张牌、运算符、第二张牌。
2. 减法结果必须是正整数；除法必须整除。
3. 四张牌必须全部使用，最终得到 24 即成功。
4. `?` 用于判断当前题目无解。
5. 底部三个按钮依次是“下一题”“重置”“答案”。

## 文件结构

- `index.html`：直接打开的游戏页面。
- `style.css`：桌面端、手机端布局及触摸效果。
- `game.js`：24点规则、求解和演示逻辑。
- `assets/page-background.webp`：独立页面纸张背景。
- `assets/stage-desktop.png`：透明桌面游戏容器。
- `assets/stage-mobile.png`：透明手机游戏容器。
- `assets/title-plaque.webp`：独立标题前景层。
- `assets/card-1.png` 至 `card-4.png`：统一画布的四张卡片。
- `assets/operator-*.png`、`assets/no-solution.png`：五个运算区按钮图。
- `assets/nav-*.png`：三个底部导航按钮图。
- `preview/`：桌面端与手机端静态预览。

## 规则说明

- A = 1、J = 11、Q = 12、K = 13。
- 只允许正整数中间结果，不允许负数、0 或分数。
- 除法只有在能够整除时才可执行。
- “重置”和“答案”不会重新随机花色；只有“下一题”会重新抽牌。
